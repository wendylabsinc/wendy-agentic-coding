---
description: Configure or explain the Wendy MCP server for Claude Code, Claude Desktop, Codex, or other MCP hosts.
argument-hint: [setup, serve, device hostname, Claude/Codex config, or MCP error]
---

Use the `wendy-mcp-setup` skill. Configure or debug Wendy MCP for:

`$ARGUMENTS`

Verify `wendy --version` first. Use `wendy mcp setup` for supported automatic setup, or configure a stdio MCP server that runs `wendy mcp serve`, optionally with `--device <hostname>`.
