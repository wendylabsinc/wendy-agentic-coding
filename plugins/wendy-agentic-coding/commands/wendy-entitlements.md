---
description: Choose, review, or debug `wendy.json` entitlements for a Wendy app.
argument-hint: [app description, wendy.json path, or runtime issue]
---

Use the `wendy-entitlements` skill. Create or review entitlements for:

`$ARGUMENTS`

Base recommendations on the app's actual device/network/storage needs and the runtime behavior in `wendy-agent/go/internal/agent/oci/entitlements.go`.
