---
description: Diagnose a WendyOS or `wendy run` issue across CLI, agent, OS image, and app layers.
argument-hint: [hostname, failing command, log, or symptom]
---

Use the `wendy-device-debug` skill. Diagnose this issue:

`$ARGUMENTS`

Start with the exact command or log if provided. If live-device access is available, inspect the device early with read-only checks before proposing code changes.
